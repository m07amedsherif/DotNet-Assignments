#region Part1Q1
/*
 * an interface is a contract that the class which implement the interface must follow
 * to prevent the tight coupling
 * loosliy coupling, using the polymorphism without inheritance, enable the multiple inheritance 
 */
#endregion

#region Part1Q2
/*
 * A) method name conflict: the class implements two interfaces which has a method in common, the class handels it by just implementing the method because it sees it as the same method because there is difference between them
 * B) i will fix it by explicitly implementing the method of each of the interfaces, itis called Explicit Implementation
 * C) no u can't becasue u need to clarify which function u r calling, we call each version by using interface reference
 */
#endregion

#region Part1Q3
/*
 * shallow copy: we make a copy of the object but the refrences of the reference of the reference types and the values of the value types
 * deep copy: we make a copy of the object by creating a new object with the same values of the first object which make a fully independent duplicate
 * the risk is that the two object reference type fields will be referencing the same value in the memory, so when we change the value in one object it will also be changed in the second one
 */
#endregion

#region Part1Q4
/*
 * Dev - Testing
 * QA - Testing
 * because e2 is a shallow copy of e1 so the reference values is shared between the two objects 
 * the title changed because it is a string and the string is immutable so when we change it it only creates another string inside the memory
 */
#endregion